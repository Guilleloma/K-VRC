# Versión mínima requerida de openai para usar la nueva interfaz
openai>=1.0.0

# Se requiere para la captura de audio en tiempo real
pyaudio==0.2.11

# Usado en el procesamiento numérico, por ejemplo en el STT
numpy==1.23.5

# OpenCV para la captura y procesamiento de imágenes en el módulo vision
# Comentado porque es opcional en Raspberry Pi
# opencv-python>=4.7.0.72

# pydub para la manipulación y reproducción de audio en el TTS
pydub>=0.25.1

# Pantalla OLED y componentes de hardware para Raspberry Pi
Adafruit-SSD1306>=1.6.2
Adafruit-Blinka>=6.0.0
adafruit-circuitpython-ssd1306>=1.5.0
RPi.GPIO>=0.7.0

# Frameworks para chat y modelos de lenguaje
langchain>=0.0.335
langchain-openai>=0.0.2
python-dotenv>=1.0.0

# Comunicación websocket para streaming de voz
websockets>=11.0.0

# Reintentos automatizados para manejar errores de conexión
tenacity>=8.2.0

# ========================
# DEPENDENCIAS DEL SISTEMA
# ========================
# Ejecutar en Raspberry Pi:
# sudo apt-get update
# sudo apt-get install -y mpg123 alsa-utils libopenblas-dev libatlas-base-dev ffmpeg libavcodec-extra
# Dependencias del sistema (instalar manualmente):
# sudo apt-get install mpg123
